# Repository notes

This project intentionally lives in its own Git repository.

The portfolio repository should contain only a lightweight project page/card
that links to the published standalone project. The analysis source, data,
committee snapshot logic, and deployment workflow belong here.

Generated files are ignored because the GitHub Actions workflow rebuilds them.
